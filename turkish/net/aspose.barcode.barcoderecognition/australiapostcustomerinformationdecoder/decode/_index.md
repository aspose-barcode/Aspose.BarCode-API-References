---
title: Decode
second_title: Aspose.BarCode for .NET API Referansı
description: AustraliaPost sembolojisinden Müşteri Bilgileri Alanının Kodunu Çözün. NTable ve CTable kodlamasından farklı veri yorumlamaları için kullanılabilir. Veriler bir dizi çubuk değeri olarak sağlanır 0 1 2 veya 3.
type: docs
weight: 10
url: /tr/net/aspose.barcode.barcoderecognition/australiapostcustomerinformationdecoder/decode/
---
## AustraliaPostCustomerInformationDecoder.Decode method

AustraliaPost sembolojisinden Müşteri Bilgileri Alanının Kodunu Çözün. NTable ve CTable kodlamasından farklı veri yorumlamaları için kullanılabilir. Veriler, bir dizi çubuk değeri olarak sağlanır: 0, 1, 2 veya 3.

```csharp
public string Decode(string customerInformationField)
```

| Parametre | Tip | Tanım |
| --- | --- | --- |
| customerInformationField | String | Ham çubuk değerleri satırı olarak kodlanmış Müşteri Bilgileri Alanı: 0, 1, 2 veya 3 |

### Geri dönüş değeri

kodu çözülen Müşteri Bilgileri Alanı dizesi

### Örnekler

Bu örnek, AustraliaPostCustomerInformationDecoder interface ile verilerin kodunun nasıl çözüleceğini gösterir.

```csharp
[C#]
string[] N_Table = { "00", "01", "02", "10", "11", "12", "20", "21", "22", "30" };
public string Decode(string customerInformationField)
{
    StringBuilder bd = new StringBuilder();
    for (int i = 0; customerInformationField.Length > i; i += 2)
    {
        if (customerInformationField.Length >= i + 2)
        {
            string tmp = customerInformationField.Substring(i, 2);
            for (int j = 0; N_Table.Length > j; j++)
            {
                if (N_Table[j].Equals(tmp))
                {
                    bd.Append(j);
                    break;
                }
            }
        }
    }
     return bd.ToString();
}
```

### Ayrıca bakınız

* interface [AustraliaPostCustomerInformationDecoder](../../australiapostcustomerinformationdecoder)
* ad alanı [Aspose.BarCode.BarCodeRecognition](../../australiapostcustomerinformationdecoder)
* toplantı [Aspose.BarCode](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.BarCode.dll -->
