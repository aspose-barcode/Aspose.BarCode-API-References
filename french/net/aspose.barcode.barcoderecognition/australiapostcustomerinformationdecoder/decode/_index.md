---
title: Decode
second_title: Référence de l'API Aspose.BarCode pour .NET
description: Décoder le champ dinformations client à partir de la symbologie AustraliaPost. Peut être utilisé pour différentes interprétations de données à partir de lencodage NTable et CTable. Les données sont fournies sous la forme dune ligne de valeurs de barres  0 1 2 ou 3.
type: docs
weight: 10
url: /fr/net/aspose.barcode.barcoderecognition/australiapostcustomerinformationdecoder/decode/
---
## AustraliaPostCustomerInformationDecoder.Decode method

Décoder le champ d'informations client à partir de la symbologie AustraliaPost. Peut être utilisé pour différentes interprétations de données à partir de l'encodage NTable et CTable. Les données sont fournies sous la forme d'une ligne de valeurs de barres : 0, 1, 2 ou 3.

```csharp
public string Decode(string customerInformationField)
```

| Paramètre | Taper | La description |
| --- | --- | --- |
| customerInformationField | String | Le champ d'informations client encodé sous la forme d'une ligne de valeurs de barres brutes : 0, 1, 2 ou 3 |

### Return_Value

la chaîne de champ d'informations client décodée

### Exemples

Cet exemple montre comment décoder des données avec l'interface AustraliaPostCustomerInformationDecoder

```csharp
[C#]
string[] N_Table = { "00", "01", "02", "10", "11", "12", "20", "21", "22", "30" };
public string Decode(string customerInformationField)
{
    StringBuilder bd = new StringBuilder();
    for (int i = 0; customerInformationField.Length > i; i += 2)
    {
        if (customerInformationField.Length >= i + 2)
        {
            string tmp = customerInformationField.Substring(i, 2);
            for (int j = 0; N_Table.Length > j; j++)
            {
                if (N_Table[j].Equals(tmp))
                {
                    bd.Append(j);
                    break;
                }
            }
        }
    }
     return bd.ToString();
}
```

### Voir également

* interface [AustraliaPostCustomerInformationDecoder](../../australiapostcustomerinformationdecoder)
* espace de noms [Aspose.BarCode.BarCodeRecognition](../../australiapostcustomerinformationdecoder)
* Assemblée [Aspose.BarCode](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.BarCode.dll -->
