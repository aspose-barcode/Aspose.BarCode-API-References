---
title: DetectEncoding
second_title: Aspose.BarCode untuk .NET API Referensi
description: Bendera yang memaksa mesin untuk mendeteksi pengkodean teks kode untuk kumpulan kode Unicode. Nilai default adalah true.
type: docs
weight: 30
url: /id/net/aspose.barcode.barcoderecognition/barcodesettings/detectencoding/
---
## BarcodeSettings.DetectEncoding property

Bendera yang memaksa mesin untuk mendeteksi pengkodean teks kode untuk kumpulan kode Unicode. Nilai default adalah true.

```csharp
public bool DetectEncoding { get; set; }
```

### Nilai properti

Bendera yang memaksa mesin untuk mendeteksi pengkodean teks kode untuk kumpulan kode Unicode

### Contoh

Contoh ini menunjukkan cara mendeteksi penyandian teks dengan cepat jika DetectEncoding diaktifkan

```csharp
[C#]
using (MemoryStream ms = new MemoryStream())
{
    using (BarcodeGenerator generator = new BarcodeGenerator(EncodeTypes.QR, "Слово"))
    {
        generator.Parameters.Barcode.QR.CodeTextEncoding = Encoding.UTF8;
        generator.Save(ms, BarCodeImageFormat.Png);
    }
    //mendeteksi penyandian untuk kumpulan kode Unicode diaktifkan
    ms.Position = 0;
    using (BarCodeReader reader = new BarCodeReader(ms, DecodeType.QR))
    {
        reader.BarcodeSettings.DetectEncoding = true;
        foreach (BarCodeResult result in reader.ReadBarCodes())
            Console.WriteLine("BarCode CodeText: " + result.CodeText);
    }
    // deteksi penyandian dinonaktifkan
    ms.Position = 0;
    using (BarCodeReader reader = new BarCodeReader(ms, DecodeType.QR))
    {
        reader.BarcodeSettings.DetectEncoding = false;
        foreach (BarCodeResult result in reader.ReadBarCodes())
            Console.WriteLine("BarCode CodeText: " + result.CodeText);
    }
}
[VB.NET]
Using ms As New MemoryStream
    Using generator As New BarcodeGenerator(EncodeTypes.QR, "Слово")
        generator.Parameters.Barcode.QR.CodeTextEncoding = System.Text.Encoding.UTF8
        generator.Save(ms, BarCodeImageFormat.Png)
    End Using
    'mendeteksi penyandian untuk kumpulan kode Unicode diaktifkan
    ms.Position = 0
    Using reader As New BarCodeReader("c:\test.png", DecodeType.Code39Standard, DecodeType.Code128)
        reader.BarcodeSettings.DetectEncoding = True
        For Each result As BarCodeResult In reader.ReadBarCodes()
            Console.WriteLine("BarCode CodeText: " + result.CodeText)
        Next
    End Using
    'deteksi penyandian dinonaktifkan
    ms.Position = 0
    Using reader As New BarCodeReader("c:\test.png", DecodeType.Code39Standard, DecodeType.Code128)
        reader.BarcodeSettings.DetectEncoding = False
        For Each result As BarCodeResult In reader.ReadBarCodes()
            Console.WriteLine("BarCode CodeText: " + result.CodeText)
        Next
    End Using
End Using
```

### Lihat juga

* class [BarcodeSettings](../)
* ruang nama [Aspose.BarCode.BarCodeRecognition](../../barcodesettings/)
* perakitan [Aspose.BarCode](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.BarCode.dll -->
